# Latent Semantic Indexing / Analysis (LSI / LSA)
Measure similarity between text documents. 

Works best with news articles since the k-dimensional subspace (principal components) represents news articles (is fitted using news articles).